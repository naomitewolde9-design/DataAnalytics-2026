# Variable Examples 
# Customer Car Insurance 
customer_id = '687' # Value needs a str variable because ids are usually a number 
customer_name = 'Naomi Tewolde' # Value needs a str variable since it's an actual set name 
customer_gender = 'Female'  # value needs a str variable 
customer_dob = '02/13/2003' # value needs a str even though the variable is somewhat numerical 
# it's still needs a str variable 
drivers_license_number = '10101010' # value needs a str variable because it's a set value for 
# a specific customer
auto_policy_number = '12121212' # needs a str variable because it's a set value  

str_1 = 'Customer ID'
str_2 = "Customer’s name"
str_3 = 'Customer gender'
str_4 = 'Customer date of birth'
str_5 = "Driver's license number"
str_6 = 'Auto policy number'

print('Customer Auto Insurance: ')
print(str_1 + ': ' + customer_id)
print(str_2 + ': ' + customer_name)
print(str_3 + ': ' + customer_gender)
print(str_4 + ': ' + customer_dob)
print(str_5 + ': ' + drivers_license_number)
print(str_6 + ': ' + auto_policy_number)


#MY INFO 

first_name = 'Naomi'
last_name = 'Tewolde'
city = 'Charlotte'
state = 'North Carolina'

print('My Personal Info: ')
print('First and Last name: ' + first_name + ' ' + last_name)
print('Location: ' + city + ', ' + state)


#PYTHON RESERVED WORDS

# a) What is the full list of reserved words that can’t be used for variable names?

# ANSWER: True, False and None cannot be used as variable names or function names.

# b) Pick 5 of these words and review the explanation for how it is used as a keyword in 
# Python. Add these 5 definitions as # comments to your exercise document. Put ^^ 
# around any terms that you are not familiar with.

# ANSWER: 

# False: the values are not the same (==) oppisite goes for True statements, def: creates place holders so you can add
# variable to a function later on into the code, elif: is used in nested conditionals, 
# if: is also used in conditionals. I'm familar with these but I'm not an expert

# raise  None  del  import  return  in  try  and  else  is  while  as  except with lambda
# ^^     ^^    ^^   ^^      ^^      ^^  ^^   ^^   ^^    ^^  ^^     ^^  ^^     ^^   ^^